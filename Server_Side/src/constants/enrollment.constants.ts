// Các hằng số nghiệp vụ đăng ký học phần.
// Client (RegistrationScreen) hiển thị cùng giá trị trần này — nếu thay đổi
// phải cập nhật đồng bộ MAX_CREDITS trong client_side.

// Tổng số tín chỉ tối đa một sinh viên được đăng ký trong một học kỳ
export const MAX_CREDITS_PER_SEMESTER = 24;
